This is a collection of images that can be used as templates for creating webOS icons.

webOS Icon Templates.psd contains:
* a grey drop shadow that is the same as the drop shadow on unpressed toolbar icons
* a blue drop shadow that is the same as the drop shadow on pressed toolbar icons

webOS Icon Gradient - Dark.png is the same gradient that dark toolbar icons use in their unpressed state

webOS Icon Gradient - Light.png is the same gradient that light toolbar icons use in their unpressed state

